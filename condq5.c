#include<stdio.h>
int main()
{
    int m;
    printf("Enter the value of m:");
    scanf("%d",&m);
    if(m>0)
    printf("the value of n = 1");
    else if(m==0)
    printf("the value of n = 0");
    else
    printf("the value of n = -1");
    return 0;
}